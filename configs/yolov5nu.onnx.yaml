model:
  type: yolov5
  weights: weights/yolov5nu.onnx
  input_size: [640, 640]
  conf_threshold: 0.25
  iou_threshold: 0.45

backend:
  type: onnxruntime
  providers:
    - CUDAExecutionProvider
    - CPUExecutionProvider

inference:
  sinks:
    - visualize
  save_dir: outputs
